import {
  articles,
  assistantSettings,
  beforeAfterCases,
  clinic,
  conditions,
  doctorProfile,
  equipmentGalleryImages,
  galleryImages,
  hairTransformations,
  homepageContent,
  mediaLogos,
  proofStats,
  reviewSummary,
  recognitionItems,
  socialLinks,
  socialStats,
  skinTransformations,
  testimonials,
  treatments,
  videoItems,
} from "@/data/seed";
import type { MediaRequirement, Review } from "@/types/cms";

export const fallbackReviews: Review[] = [];

export const fallbackMediaRequirements: MediaRequirement[] = [
  {
    groupKey: "doctor_photos",
    groupLabel: "Doctor Photos",
    requestKey: "doctor-editorial-portrait",
    title: "Editorial doctor portrait",
    description:
      "Replace abstract doctor areas with real approved photography of Dr. Satyarth Prakash.",
    idealDimension: "2400x3000",
    minimumDimension: "1400x1800",
    orientation: "Portrait",
    usageLocation: "Hero, About, Doctor authority",
    mediaType: "image",
    status: "missing",
    notes: "Do not use a fake doctor portrait.",
  },
  {
    groupKey: "clinic_interiors",
    groupLabel: "Clinic Interiors",
    requestKey: "clinic-reception-hero",
    title: "Clinic reception / entrance",
    description:
      "Premium clinic ambience image for credibility and conversion sections.",
    idealDimension: "2400x1400",
    minimumDimension: "1600x900",
    orientation: "Landscape",
    usageLocation: "Hero collage, clinic ambience, contact",
    mediaType: "image",
    status: "missing",
  },
  {
    groupKey: "before_after_cases",
    groupLabel: "Before/After Cases",
    requestKey: "ba-consented-cases",
    title: "Consented before/after case set",
    description:
      "Matched case pairs with time gap, consent confirmation and medical disclaimer.",
    idealDimension: "1600x1200 each",
    minimumDimension: "1000x750 each",
    orientation: "Matched landscape or portrait",
    usageLocation: "Before/after section, treatment detail pages",
    mediaType: "before_after",
    status: "missing",
    notes: "Do not publish without consent.",
  },
];

export const fallbackData = {
  siteSettings: clinic,
  homepage: homepageContent,
  heroImages: homepageContent.heroImages,
  proofStats,
  treatments,
  conditions,
  articles,
  beforeAfterCases,
  hairTransformations,
  skinTransformations,
  galleryImages,
  equipmentGalleryImages,
  recognitionItems,
  videoItems,
  testimonials,
  reviews: fallbackReviews,
  socialLinks,
  socialStats,
  reviewSummary,
  mediaRequirements: fallbackMediaRequirements,
  assistantSettings,
  assistantKnowledge: {
    settings: assistantSettings,
    faqs: homepageContent.faqs,
    quickPrompts: assistantSettings.quickPrompts,
    serviceNotes: [
      clinic.tagline,
      "Book through phone, WhatsApp, contact form or the Radiance AI Assistant callback flow.",
      "The assistant gives general service guidance only and does not diagnose.",
    ],
  },
  doctorProfile,
  mediaLogos,
};

export {
  articles as fallbackArticles,
  assistantSettings as fallbackAssistantSettings,
  beforeAfterCases as fallbackBeforeAfterCases,
  clinic as fallbackSiteSettings,
  conditions as fallbackConditions,
  doctorProfile as fallbackDoctorProfile,
  equipmentGalleryImages as fallbackEquipmentGalleryImages,
  galleryImages as fallbackGalleryImages,
  hairTransformations as fallbackHairTransformations,
  homepageContent as fallbackHomepageContent,
  mediaLogos as fallbackMediaLogos,
  proofStats as fallbackProofStats,
  reviewSummary as fallbackReviewSummary,
  recognitionItems as fallbackRecognitionItems,
  socialLinks as fallbackSocialLinks,
  socialStats as fallbackSocialStats,
  skinTransformations as fallbackSkinTransformations,
  testimonials as fallbackTestimonials,
  treatments as fallbackTreatments,
  videoItems as fallbackVideoItems,
};
